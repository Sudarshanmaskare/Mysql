SELECT 
   [column_1 | expression] AS descriptive_name
FROM table_name;


SELECT
   CONCAT_WS(', ', lastName, firstname) AS `Full name`  ---------(`Full name` is alias)
FROM
   employees;
   


### table alias

SELECT 
    e.firstName, 
    e.lastName
FROM
    employees e
ORDER BY e.firstName;
